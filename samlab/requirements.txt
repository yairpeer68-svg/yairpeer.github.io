# תלויות ליבה (עובדות בכל גרסת Python נתמכת)
# פירוק images דחוסים (image.lz4) בתוך קבצי AP לפני פלאש
lz4>=4.0.0
# מנוע LOKE הישיר הניסיוני (טאב "מנוע ישיר") — תקשורת USB ישירה
# דורש גם את ספריית libusb-1.0 במערכת (ראה README, סעיף Zadig/libusb)
pyusb>=1.2.1

# הערה: samloader (הורדת קושחה) מותקן בנפרד. גרסת PyPI מוגבלת ל-Python <3.11,
# לכן משתמשים בפורק martinetd התומך ב-3.11+ (אותו CLI). run.bat עושה זאת אוטומטית:
#   pip install "https://github.com/martinetd/samloader/archive/refs/heads/master.zip"
