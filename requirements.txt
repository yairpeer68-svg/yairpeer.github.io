# התקנה רגילה:
# pip install -r requirements.txt
#
# Termux (Android):
# pkg install python rust libxml2 libxslt
# pip install -r requirements.txt
# (fpdf2 ו-redis הם אופציונליים — הבוט עובד בלעדיהם)

telethon>=1.36
aiohttp>=3.9
aiosqlite>=0.20
python-dotenv>=1.0
psutil>=5.9
beautifulsoup4>=4.12
openpyxl>=3.1
fpdf2>=2.7       ; platform_machine != "aarch64" or sys_platform != "linux"
redis>=5.0       ; extra == "redis"
