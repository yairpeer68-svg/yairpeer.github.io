Magen Phone v4.2 validation performed in the build workspace
============================================================
PASS: verify.py --strict
  115 Java files parsed
  61 XML files parsed
  29 manifest components checked
  359 resource references checked
  193 Hebrew / 193 English translation parity
  system-settings launch protection checks
  foreground-service checks
  permission-engine checks
  VPN/tamper hardening checks
  Visual Shield wiring/privacy/supply-chain checks

PASS: pure-Java compile/run smoke test for VisualDecision + VisualTemporalGuard
  strong explicit -> immediate block
  borderline sexy -> temporal consensus on second observation

NOT RUN HERE: Gradle Android unit tests / assembleDebug.
Reason: this execution environment cannot resolve services.gradle.org, so the Gradle 8.13 distribution cannot be downloaded. The Windows builder runs both testDebugUnitTest and assembleDebug on the user's machine before reporting success.

This validation is not proof that no runtime/OEM-specific bugs exist. Physical-device testing remains required.
