<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext traffic is off by default: the API carries legal documents and
  auth tokens, so plain HTTP is not acceptable in a shipped build.

  The debug-only exception below lets a developer run against a local backend
  (10.0.2.2 is the emulator's alias for the host machine). It applies solely to
  debuggable builds and never to a release APK.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
