package io.github.yairpeer.sanegor

import io.flutter.embedding.android.FlutterActivity

/**
 * Single-activity host for the Flutter engine.
 *
 * All application logic lives in Dart; this exists so the platform has an
 * entry point and so the manifest has an activity to launch.
 */
class MainActivity : FlutterActivity()
