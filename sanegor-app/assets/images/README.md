# Images

Intentionally empty. The app mark is drawn in code (`_ScalesPainter` in
`lib/features/splash/presentation/splash_screen.dart`) and the launcher icon is
a vector drawable, so there is no bitmap artwork to ship or to license.
