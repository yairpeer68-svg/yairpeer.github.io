# Ghost Eye Phone 10.1.1

This is a verification/stability release. It tightens session recovery, retry/cancel behavior, list identity handling, offline startup, and production diagnostics without changing the single-user UX.

The full owner email is not embedded in the Android source. The UI displays only the masked label `******er68` and login uses the server-side single-user owner endpoint.
