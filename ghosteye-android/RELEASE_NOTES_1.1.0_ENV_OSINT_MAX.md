# Ghost Eye Phone 1.1 — Provider Environment + OSINT Expansion

- Provider picker expanded to the enlarged server-side Provider Vault.
- Added server Provider Environment status: shows provider count and configured count without exposing secret values.
- Added IPinfo and additional optional provider entries.
- Existing OSINT/Vulnerability/Entity Graph APIs remain available.
- Android release guard passed.
- Full Gradle/Kotlin compile was not available because Gradle 8.7 is not cached in this build environment.
